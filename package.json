{
  "name": "nyaya-ai",
  "version": "1.0.0",
  "description": "Nyaya AI - Bias Detection & Fairness Evaluation System",
  "scripts": {
    "start:backend": "cd backend && npm run dev",
    "start:ml": "cd ml && python -m nyaya_ai.api_service",
    "start:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ../ml && pip install -r requirements.txt",
    "test:backend": "curl http://localhost:5000/api/v1/analyze/status",
    "test:ml": "curl http://localhost:5001/health"
  },
  "keywords": ["ai", "bias-detection", "fairness", "ml"],
  "author": "Nyaya AI Team",
  "license": "MIT"
}
